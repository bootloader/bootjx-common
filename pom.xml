<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>com.boot.jx</groupId>
	<artifactId>bootjx-common</artifactId>
	<name>bootjx-common</name>
	<packaging>pom</packaging>
	<description>Parent pom providing dependency and plugin management for applications built with Maven </description>
	<url>http://maven.apache.org</url>
	<version>2.7.18-SNAPSHOT</version>

	<organization>
		<name>Java Extended Spring Framework</name>
		<url>https://bootloader.github.io</url>
	</organization>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>2.7.18</version>
		<relativePath />
	</parent>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<java.version>11</java.version>
		<boot.jx.version>2.7.18-SNAPSHOT</boot.jx.version>
		<!-- Mongo Java driver line. Overrides the Boot 2.7.18 BOM's own 4.6.1
			pin: 4.11.5 is already verified end-to-end against this codebase and
			talks MongoDB 8.0's auth handshake, which 4.6.1 (and the pre-4.x
			"mongodb-driver" uber-jar this project started on) cannot. Spring
			Data MongoDB 3.x (brought in by the Boot 2.7.18 BOM) only ever
			depends on the modular mongodb-driver-sync/-core/bson artifacts, so
			there's no old uber-jar left on the classpath to collide with. -->
		<mongodb.driver.version>4.11.5</mongodb.driver.version>
	</properties>

	<dependencyManagement>
		<dependencies>
			<!-- Pin past the Spring Boot 2.0.5 BOM's 4.5.6: last patch of the 4.5.x
				line, pure bugfixes, zero API change. -->
			<dependency>
				<groupId>org.apache.httpcomponents</groupId>
				<artifactId>httpclient</artifactId>
				<version>4.5.14</version>
			</dependency>

			<!-- Mongo Java driver 4.11.x: overrides the Boot 2.7.18 BOM's 4.6.1.
				Spring Data MongoDB 3.x (this BOM) only ever depends on the
				modular mongodb-driver-sync/-core/bson artifacts - the legacy
				"mongodb-driver" uber-jar is gone from the dependency graph
				entirely, so no exclusion is needed anymore. mongodb-driver-legacy
				is still pinned/declared explicitly because a handful of
				DBObject/BasicDBObject call sites in bootjx-common-mongo and
				lib-postman-store depend on it directly. -->
			<dependency>
				<groupId>org.mongodb</groupId>
				<artifactId>bson</artifactId>
				<version>${mongodb.driver.version}</version>
			</dependency>
			<dependency>
				<groupId>org.mongodb</groupId>
				<artifactId>mongodb-driver-core</artifactId>
				<version>${mongodb.driver.version}</version>
			</dependency>
			<dependency>
				<groupId>org.mongodb</groupId>
				<artifactId>mongodb-driver-sync</artifactId>
				<version>${mongodb.driver.version}</version>
			</dependency>
			<dependency>
				<groupId>org.mongodb</groupId>
				<artifactId>mongodb-driver-legacy</artifactId>
				<version>${mongodb.driver.version}</version>
			</dependency>

			<dependency>
				<groupId>com.boot.jx</groupId>
				<artifactId>bootjx-extjar</artifactId>
				<version>${boot.jx.version}</version>
				<type>pom</type>
			</dependency>
			<dependency>
				<groupId>com.boot.jx</groupId>
				<artifactId>bootjx-common-utils</artifactId>
				<version>${boot.jx.version}</version>
			</dependency>
			<dependency>
				<groupId>com.boot.jx</groupId>
				<artifactId>bootjx-common-lib</artifactId>
				<version>${boot.jx.version}</version>
			</dependency>
			<dependency>
				<groupId>com.boot.jx</groupId>
				<artifactId>bootjx-common-mongo</artifactId>
				<version>${boot.jx.version}</version>
			</dependency>

			<dependency>
				<groupId>com.boot.jx</groupId>
				<artifactId>bootjx-common-db</artifactId>
				<version>${boot.jx.version}</version>
			</dependency>

			<dependency>
				<groupId>com.boot.jx</groupId>
				<artifactId>bootjx-common-linq</artifactId>
				<version>${boot.jx.version}</version>
			</dependency>

			<dependency>
				<groupId>com.boot.jx</groupId>
				<artifactId>bootjx-common-test</artifactId>
				<version>${boot.jx.version}</version>
				<scope>test</scope>
			</dependency>

		</dependencies>
	</dependencyManagement>

	<dependencies>

	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<source>11</source>
					<target>11</target>
				</configuration>
			</plugin>
		</plugins>
		<pluginManagement>
			<plugins>
				<!--This plugin's configuration is used to store Eclipse m2e settings 
					only. It has no influence on the Maven build itself. -->
				<plugin>
					<groupId>org.eclipse.m2e</groupId>
					<artifactId>lifecycle-mapping</artifactId>
					<version>1.0.0</version>
					<configuration>
						<lifecycleMappingMetadata>
							<pluginExecutions>
								<pluginExecution>
									<pluginExecutionFilter>
										<groupId>
											org.apache.maven.plugins
										</groupId>
										<artifactId>
											maven-compiler-plugin
										</artifactId>
										<versionRange>
											[3.1,)
										</versionRange>
										<goals>
											<goal>testCompile</goal>
										</goals>
									</pluginExecutionFilter>
									<action>
										<ignore></ignore>
									</action>
								</pluginExecution>
							</pluginExecutions>
						</lifecycleMappingMetadata>
					</configuration>
				</plugin>
			</plugins>
		</pluginManagement>
	</build>

	<modules>
		<!-- Common Libs -->
		<module>bootjx-common-utils</module>
		<module>bootjx-common-lib</module>
		<module>bootjx-common-db</module>
		<module>bootjx-common-mongo</module>
		<module>bootjx-common-linq</module>
		<!-- Common Servers -->
		<module>bootjx-common-test</module>
		<module>bootjx-demo-server</module>
	</modules>
	
	<distributionManagement>
	   <repository>
	     <id>github</id>
	     <name>GitHub bootloader Apache Maven Packages</name>
	     <url>https://maven.pkg.github.com/bootloader/bootloader.github.io</url>
	   </repository>
	</distributionManagement>

</project>
